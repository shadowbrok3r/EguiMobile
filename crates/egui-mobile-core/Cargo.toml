[package]
name = "egui-mobile-core"
description = "Platform-neutral core for egui mobile apps: the EguiApp trait and the Host capability bridge shared by egui-ios and egui-android."
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
egui.workspace = true
# The overflow guard reports through `log`; the platform runtimes route it to logcat / oslog.
log.workspace = true
